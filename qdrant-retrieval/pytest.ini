[pytest]
pythonpath = .
asyncio_mode = strict
testpaths = app/tests
addopts = --ignore=app/tests/test_pipeline_integration.py
markers =
    integration: full pipeline integration tests (requires all services running against production cloud)
