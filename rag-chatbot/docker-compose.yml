services:
  rag-chatbot-service:
    build: .
    container_name: rag-agentic-app
    ports:
      - "5001:5001"
    volumes:
      - .:/code
    environment:
      - QDRANT_HOST=qdrant
    command: fastapi dev app/main.py --host 0.0.0.0 --port 5001
