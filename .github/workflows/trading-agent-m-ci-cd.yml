name: Trading Agent M CI/CD Pipeline

on:
  workflow_dispatch:
    inputs:
      skip-approval:
        description: "Bypass manual approval"
        required: false
        default: "false"
  push:
    branches: ["master", "dev"]
    paths:
      - "trading-agent-m/**"
      - "deploy/**"
      - "infra/**"
      - ".github/workflows/python-reusable-workflow.yaml"
  pull_request:
    branches: ["master", "dev"]
    paths:
      - "trading-agent-m/**"
      - "deploy/**"
      - "infra/**"
      - ".github/workflows/python-reusable-workflow.yaml"
jobs:
  trading-agent-m-workflow:
    uses: ./.github/workflows/python-reusable-workflow.yaml
    with:
      working-directory: "trading-agent-m"
      cluster-name: "agentic-m-cluster"
      image-name: "trading-agent-m"
      skip-approval: ${{ inputs.skip-approval || 'false' }}
      values-file: "deploy/prod/values-trading-agent-m.yaml"
      secret-key: "TRADING_AGENT_M_SECRETS"
    secrets: inherit