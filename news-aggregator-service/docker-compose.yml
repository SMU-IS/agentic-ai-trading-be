services:
  news-aggregator:
    build: .
    command: uvicorn src.main:app --host 0.0.0.0 --port 5008 --reload
    ports:
      - "5008:5008"
    depends_on:
      - redis
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
