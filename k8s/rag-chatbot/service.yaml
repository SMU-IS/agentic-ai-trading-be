apiVersion: v1
kind: Service
metadata:
  name: rag-chatbot-service
  labels:
    app: rag-chatbot
spec:
  selector:
    app: rag-chatbot
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
